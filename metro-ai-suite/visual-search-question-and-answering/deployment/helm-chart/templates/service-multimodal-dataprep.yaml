apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.multimodalDataprep.name }}
spec:
  type: {{ .Values.multimodalDataprep.service.type }}
  ports:
    - port: {{ .Values.multimodalDataprep.service.port }}
      targetPort: {{ .Values.multimodalDataprep.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.multimodalDataprep.name }}

