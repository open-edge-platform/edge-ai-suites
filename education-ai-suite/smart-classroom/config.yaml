app:
  name: Smart Classroom
  cleanup_on_exit: true
  use_ov_genai: False # True or False, applicable for provider: openvino
  language: en # en or zh

monitoring:
  logs_dir: ./monitoring/logs
  interval: 2
  socwatch_path: monitoring/tools/socwatch_windows_v2026.0/64/socwatch.exe
  npu_exe_path: tools/level-zero/build/bin/Release/npu_utilization.exe
  execution_logs: monitoring/executionlogs

models:
  asr:
    provider: openai # openvino, funasr and openai supported
    name: whisper-small  # can be (whisper-base, whisper-small etc) or paraformer-zh
    device: CPU # CPU Recommended
    temperature: 0.0
    diarization: False
    hf_token: ""  # set via HF_TOKEN env var (do not commit)   # needed only if diarization=true
    models_base_path: "models"
    threads_limit: Null # applied only if > 0 (else defaults are used); value can be tuned based on CPU specifications
    no_speech_threshold: 0.6
    logprob_threshold: -1.0
    min_duration_sec: 0.25
    min_words: 2
  
  diarization: #Only applicable when diarization is set to True for ASR
    provider: "huggingface"
    name: "pyannote/speaker-diarization-community-1"
    models_base_path: "models"

  summarizer:
    provider: openvino 
    name: Qwen/Qwen3-8B 
    device: GPU # GPU or CPU
    weight_format: int4 # supports fp16, int4, int8 (Recommended)
    max_new_tokens: 5120
    temperature: 0.0 # 0.5 default
    use_cache: True
    models_base_path: "models"
    mode: dialog   # dialog | teacher | hybrid
    system_prompt: 
      en:

        Dialog: |
          Summarize the classroom transcript.

          Format:
          TEACHER = instructor
          STUDENT_XX = students

          Instructions:
          - Provide a full dialog summary.
          - Preserve the flow of discussion.
          - Include both teacher explanations and questions.
          - Ignore filler or repetition.

          Output valid Markdown using exactly these sections:

          ## Teacher Summary
          - ...

          ## Questions
          - ...

          ## Key Takeaways
          - ...

          ## Session Outline
          - ...

          Rules:
          - Bullet points only.
          - Do not quote transcript text.
          - Do not add extra headings.


        Teacher: |
          Summarize only the TEACHER explanations from the transcript.

          Instructions:
          - Ignore student dialog completely.
          - Focus strictly on teacher explanations.
          - Do not invent missing content.
          - If a section has no content, write "None".

          Output valid Markdown using exactly these sections:

          ## Teacher Summary
          - ...

          ## Questions
          - None

          ## Key Takeaways
          - ...

          ## Session Outline
          - ...

          Rules:
          - Bullet points only.
          - Do not quote transcript text.
          - Do not add extra headings.


        Hybrid: |
          Summarize with focus on TEACHER explanations,
          but use STUDENT questions as supporting context.

          Instructions:
          - Teacher explanations are primary.
          - Include student questions only when they clarify context.
          - Do not invent content.
          - Ignore filler.

          Output valid Markdown using exactly these sections:

          ## Teacher Summary
          - ...

          ## Questions
          - ...

          ## Key Takeaways
          - ...

          ## Session Outline
          - ...

          Rules:
          - Bullet points only.
          - Do not quote transcript text.
          - Do not add extra headings.
      zh:
        Dialog: |
          ?????????

          ????:
          ?? = ????
          ??_XX = ????

          ??:
          - ??????????
          - ??????????
          - ????????????
          - ???????????????

          ?????????????? Markdown:

          ## ????
          - ...

          ## ??
          - ...

          ## ????
          - ...

          ## ????
          - ...

          ??:
          - ??????????
          - ?????????????
          - ?????????

        Teacher: |
          ?????? ?? ??????

          ??:
          - ?????????
          - ????????????
          - ??????????
          - ??????????,? "None"?

          ?????????????? Markdown:

          ## ????
          - ...

          ## ??
          - None

          ## ????
          - ...

          ## ????
          - ...

          ??:
          - ??????????
          - ?????????????
          - ?????????


        Hybrid: |
          ? ?? ?????????,
          ??????? ?? ??????????

          ??:
          - ??????????
          - ???????????????????
          - ???????
          - ???????????

          ?????????????? Markdown:

          ## ????
          - ...

          ## ??
          - ...

          ## ????
          - ...

          ## ????
          - ...

          ??:
          - ??????????
          - ?????????????
          - ?????????

    model_hub: huggingface # huggingface or modelscope

  va:
    models_base_path: "models"
    front_pose_model: yolov8m-pose # yolov8m-pose/yolo11m-pose/yolo26m-pose
    back_pose_model: yolov8s-pose # yolov8s-pose/yolo11s-pose/yolo26s-pose
    threshold: 0.5 # confidence threshold for YOLO detections
  
  embedding:
    name: "BAAI/bge-large-en-v1.5"
    min_score: 0.65

  ocr:
    enabled: false # Set to false to disable OCR features
    provider: openvino # native or openvino
    device: CPU # CPU or GPU
    concurrency: 2    # CapabilityRunner max_concurrency (worker pool size)
    queue_max: 16     # CapabilityRunner queue depth before 503 backpressure
    model_dir: models/ocr # Base directory for OCR models
    ocr_version: PP-OCRv6
    det_model: PP-OCRv6_small_det
    rec_model: PP-OCRv6_small_rec
    cls_model: PP-LCNet_x1_0_doc_ori


mindmap:
  system_prompt:
    en: |
      Generate a comprehensive jsMind JSON structure that accurately captures ALL key concepts from the summary. Follow these strict rules:
 
      1. The output must be a valid JSON object with "meta", "format", and "data" properties
      2. Set format to "node_tree"
      3. There must be exactly ONE root node with id "root"
      4. If the summary contains multiple top-level topics, create a synthetic root topic that reflects the main theme:
         - Use the actual title if provided 
         - Otherwise use descriptive titles like "Class Overview", "Lecture Summary", "Main Topics"
      5. Each node must have "id" and "topic" properties
      6. Child nodes are placed in a "children" array
      7. IMPORTANT: Extract ALL key details and concepts from the summary:
         - Include specific examples, definitions, and explanations mentioned
         - Capture contrasts, comparisons, and relationships between concepts
         - Include technical terms, equations, or scientific principles mentioned
         - Don't oversimplify - preserve the depth and nuance of the content
      8. Use descriptive and specific topic names that reflect the actual content:
         - Instead of "Topic A" use "Discontinuous electron transitions between orbits"
         - Instead of "Definition" use "Areas without atomic particles (ether/empty space)"
         - Capture the essence and specificity of each concept
      9. Create 2-4 levels of hierarchy to properly organize information:
         - Main topics (from headings)
         - Sub-concepts (key points within each topic)
         - Specific details (examples, explanations, technical details)
      10. Ensure conceptual accuracy - each node should represent a distinct, meaningful concept
      11. Do NOT include any markdown formatting, code fences, or explanatory text
      12. Output only the raw JSON structure
      13. Always return a valid JSON structure.
 
      Example of GOOD specificity:
      {
        "meta": {
          "name": "physics_lecture",
          "author": "ai_assistant", 
          "version": "1.0"
        },
        "format": "node_tree",
        "data": {
          "id": "root",
          "topic": "Quantum Mechanics and Wave Theory",
          "children": [
            {
              "id": "wave_particle_duality",
              "topic": "Wave-Particle Duality",
              "children": [
                {"id": "photon_behavior", "topic": "Photons exhibit both wave and particle properties"},
                {"id": "double_slit", "topic": "Double-slit experiment demonstrates interference"},
                {"id": "measurement_effect", "topic": "Measurement collapses wave function"}
              ]
            },
            {
              "id": "uncertainty_principle",
              "topic": "Heisenberg Uncertainty Principle",
              "children": [
                {"id": "position_momentum", "topic": "Cannot precisely know both position and momentum"},
                {"id": "mathematical_formulation", "topic": "?x·?p = ?/2 mathematical relationship"},
                {"id": "quantum_limitations", "topic": "Fundamental limit of quantum measurements"}
              ]
            }
          ]
        }
      }

    zh: |
      ?????jsMind JSON??,???????????????????????:
 
      1. ???????"meta"?"format"?"data"?????JSON??
      2. ?format???"node_tree"
      3. ????????id?"root"????
      4. ??????????????,??????????????:
         - ????????????(?"????????")
         - ??????????"????"?"????"?"????"
      5. ???????"id"?"topic"??
      6. ?????"children"???
      7. ??:???????????????:
         - ???????????????
         - ???????????????
         - ?????????????????
         - ?????? - ????????????
      8. ????????????????????:
         - ??"??A"??"????????????"
         - ??"??"??"???????(??/??)"
         - ?????????????
      9. ??2-4???????????:
         - ????(????)
         - ???(?????????)
         - ????(??????????)
      10. ??????? - ??????????????????
      11. ??????markdown????????????
      12. ?????JSON??
      13. ???????JSON???
 
      ???????:
      {
        "meta": {
          "name": "????",
          "author": "??????",
          "version": "1.0"
        },
        "format": "node_tree", 
        "data": {
          "id": "root",
          "topic": "?????????",
          "children": [
            {
              "id": "?????",
              "topic": "?????",
              "children": [
                {"id": "????", "topic": "??????????????"},
                {"id": "????", "topic": "??????????"},
                {"id": "????", "topic": "?????????"}
              ]
            },
            {
              "id": "??????",
              "topic": "?????????",
              "children": [
                {"id": "????", "topic": "?????????????"},
                {"id": "?????", "topic": "?x·?p = ?/2 ????"},
                {"id": "????", "topic": "?????????"}
              ]
            }
          ]
        }
      }
  min_token: 20

audio_preprocessing:
  chunk_duration_sec: 15
  silence_threshold: -35  # in dB
  silence_duration: 0.3   # minimum silence length in seconds
  search_window_sec: 1    # how far to look for silence if no silence exactly at chunk boundary
  chunk_output_path: chunks/

audio_util:
  max_size_mb: 300
  allowed_extensions:
    - .wav
    - .mp3
    - .m4a
  chunk_size: 52428800   # 1024 * 1024 * 50 = 50MB

pipeline:
  delete_chunks_after_use: true

va_pipeline:
  mediamtx_path: components/va/bin/mediamtx
  plugin_path: components/va
  rtsp_codec: h264 # h264 or h265
  output_rtsp_url: rtsp://127.0.0.1:8554
  hls_base_url: http://127.0.0.1:8888
  webrtc_base_url: http://127.0.0.1:8889
  stream_protocol: webrtc # webrtc or hls

  pose_statistics:
    min_frames_for_transition: 3            # frames to confirm raise-hand for identified students
    min_frames_for_transition_unid: 15      # frames to confirm raise-hand for unidentified students
    absence_threshold: 90                   # forget student after unseen for N frames (90 ˜ 3s)
    min_stand_frames: 10                    # frames a student must be seen before counting stand-up (10 ˜ 0.3s)
    center_dist_threshold: 0.1              # how close two bounding boxes must be to count as same person (0~1)
    unidentified_max: 50                    # max unidentified students to track
    stale_unidentified_threshold: 30        # remove unidentified student after unseen for N frames

content_search:
  host_addr: "127.0.0.1"
  port: 9011
  video_summarization_enabled: true

  chromadb:
    host: "127.0.0.1"
    port: 9090
    data_dir: './data/chroma_data'

  storage:
    data_dir: './data/local_storage'
    bucket: "content-search"
    document_max_mb: 100    # maximum upload size for documents (MB)
    video_max_mb: 1024      # maximum upload size for videos (MB)

  vlm:
    model_name: "Qwen/Qwen2.5-VL-3B-Instruct"
    host_addr: "127.0.0.1"
    port: 9900
    device: "GPU"
    weight_format: "int8"
    ingest_enabled: true

  video_preprocess:
    host_addr: "127.0.0.1"
    port: 8001
    vlm_timeout_seconds: 300
    chunk_duration_s: 30
    chunk_overlap_s: 4
    max_num_frames: 8
    frame_width: 0
    frame_height: 0

  file_ingest:
    host_addr: "127.0.0.1"
    port: 9990
    collection_name: "content-search"
    doc_embedding_device: "CPU"
    visual_embedding_model: "CLIP/clip-xlm-roberta-base-vit-b-32"
    doc_embedding_model: "intfloat/multilingual-e5-small"
    frame_extract_interval: 30      # for video: extract a frame every N frames
    frame_extract_interval_sparse: 90  # for long videos (>20min): extract a frame every N frames
    do_detect_and_crop: false        # for video/image: run object detection and crop before embedding
    document_parser:
      chunk_method: "semantic"              # "fixed": split by character count; "semantic": split by topic/meaning using embeddings
      chunk_size: 250                    # (fixed mode) max characters per chunk
      chunk_overlap: 50                  # (fixed mode) overlapping characters between consecutive chunks
      semantic_min_chunk_size: 250       # (semantic mode) chunks shorter than this are merged into the next one
      semantic_breakpoint_percentile: 85 # (semantic mode) higher = fewer, larger chunks; lower = more, smaller chunks (range: 0-100)
      semantic_buffer_size: 2            # (semantic mode) how many neighboring sentences to group when comparing similarity; larger = less sensitive to noise
    reranker:
      model: "BAAI/bge-reranker-base"
      device: "GPU"
      dedup_time_threshold: 5    # seconds — frames closer than this from the same video are merged
      overfetch_multiplier: 3    # retrieve top_k * N candidates from DB before post-processing

  qa:
    max_context: 5          # number of top chunks retrieved from ChromaDB to use as RAG context
    max_tokens: 1024        # maximum tokens the VLM can generate for an answer (prevents mid-sentence cutoff)
    max_history_turns: 3    # number of prior conversation turns (user+assistant pairs) sent to the VLM
    context_window: 16384   # token budget for dynamic context selection; 
    retrieval_score_threshold: 85  # minimum relevance score (0-100) a chunk must have to be used as context

# -- Telegram integration (OpenClaw bridge) ------------------------------------
# Set enabled: true and fill in bot_token + chat_id to activate.
# Smart Classroom will send session data to the shared Telegram group after
# each pipeline stage completes; OpenClaw listens on the Linux side and
# answers teacher / parent queries from that same group.
telegram:
  enabled: false                          # flip to true to activate
  api_id: 0                               # from my.telegram.org ? Your Apps
  api_hash: "YOUR_API_HASH_HERE"          # from my.telegram.org ? Your Apps
  session_string: "YOUR_SESSION_STRING"   # generated by generate_session.py
  chat_id: "YOUR_CHAT_ID_HERE"            # negative number for the group chat
  class_name: "Class 10A - Physics"       # displayed in every Telegram message
  proxy:                                  # remove or leave host empty to disable
    host: ""                              # e.g. proxy.company.com
    port: 8080                            # proxy port
    type: http                            # http or socks5
    username: ""                          # leave empty if no auth required
    password: ""                          # leave empty if no auth required

# -- SCP integration (OpenClaw bridge — alternative to Telegram) --------------
# Use when the Windows machine cannot reach Telegram's servers directly.
# Prerequisites:
#   1. OpenSSH client installed on Windows (scp.exe / ssh.exe on PATH).
#   2. Generate an SSH key pair on Windows:  ssh-keygen -t ed25519
#   3. Append the public key to ~/.ssh/authorized_keys on the Linux machine.
#   4. Optionally add a Host alias to %USERPROFILE%\.ssh\config so you can
#      refer to the Linux machine by a short name (e.g. "ptl").
#
# Only one of  telegram.enabled  or  scp_sender.enabled  should be true.
scp_sender:
  enabled: false                          # flip to true to activate
  host: "ptl"                             # SSH alias from ~/.ssh/config, or IP/hostname
  identity_file: ""                       # path to private key, e.g. C:\Users\you\.ssh\id_ed25519
                                          # leave empty to use the SSH default (~/.ssh/id_*)
  remote_base_path: "/home/intel/smart_classroom_incoming"  # landing dir on Linux
  class_name: "Class 10A - Physics"       # included in session_meta.json
