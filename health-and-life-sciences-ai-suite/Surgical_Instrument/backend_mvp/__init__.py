"""Surgical Instrument backend (Flask 3.0).

`backend_mvp.app`         — production app (Phase 3).
`backend_mvp.mock_server` — mock server with canned status, fake metrics
                            and a synthesized MJPEG stream so the UI can
                            be developed and demoed offline.
"""
