# Core ML/CV Dependencies
# -----------------------
# Why: TensorFlow for MTTS-CAN model inference
tensorflow==2.15.0

# Why: OpenCV for video reading and image processing
opencv-python==4.9.0.80

# Why: NumPy for array operations (used by model)
numpy==1.26.3

# Why: SciPy for signal processing (bandpass filters, FFT)
scipy==1.11.4

# gRPC Dependencies (MUST match aggregator-service version)
# ----------------------------------------------------------
# Why: gRPC protocol for streaming Vital messages to aggregator
grpcio==1.60.0

# Why: gRPC tools to generate Python code from .proto files
grpcio-tools==1.60.0

# Why: Protocol Buffers (protobuf) for message serialization
protobuf==4.25.0

# Configuration & Utilities
# --------------------------
# Why: Parse config.yaml file
pyyaml==6.0.1

# Why: Colored console logging for better debugging
colorlog==6.8.0

# Why: Date/time parsing
python-dateutil==2.8.2

# Why: HTTP downloads for model/video auto-download
requests==2.31.0

# Why: Progress bars for downloads
tqdm==4.66.1